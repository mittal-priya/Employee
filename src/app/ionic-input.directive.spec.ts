import { IonicInputDirective } from './ionic-input.directive';

describe('IonicInputDirective', () => {
  it('should create an instance', () => {
    const directive = new IonicInputDirective();
    expect(directive).toBeTruthy();
  });
});
